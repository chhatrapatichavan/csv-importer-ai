# csv-importer-ai
AI CSV Importer is a smart CRM data import application that allows users to upload CSV files, preview records, validate data, automatically map fields, and export processed results in JSON or CSV format. Built with React, TypeScript, Node.js, Express,  to simplify CRM data migration.
